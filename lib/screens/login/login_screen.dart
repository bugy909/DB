import 'package:flutter/material.dart';

import 'package:psk_analytics/screens/login/components/body.dart';

/// A login screen with an email and password input field.
class LoginScreen extends StatelessWidget {
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
