import 'package:flutter/material.dart';

import 'package:psk_analytics/screens/exercises/shotScore/components/body.dart';

/// TODO Finish design
class ShotScore extends StatelessWidget {
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}